import ProfileScreen from '@/screens/ProfileScreen';

export const metadata = {
  title: 'Profile',
  description: 'Profile',
};

const Home = () => {
  return <ProfileScreen />;
};

export default Home;
