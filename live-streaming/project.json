{
  "name": "live-streaming",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "live-streaming",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/live-streaming"
      },
      "configurations": {
        "development": {
          "outputPath": "live-streaming"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "live-streaming:build",
        "dev": true,
        "port": 4202
      },
      "configurations": {
        "development": {
          "buildTarget": "live-streaming:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "live-streaming:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "live-streaming:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "live-streaming/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
