{
  "name": "socialweb-profiles-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backends/socialweb-profiles-service",
  "projectType": "application",
  "tags": ["type:backend", "scope:social-network", "language:go"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go run main.go",
        "cwd": "apps/backends/socialweb-profiles-service"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o dist/service .",
        "cwd": "apps/backends/socialweb-profiles-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go test ./...",
        "cwd": "apps/backends/socialweb-profiles-service"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t tyrian/socialweb-profiles-service .",
        "cwd": "apps/backends/socialweb-profiles-service"
      }
    }
  }
}

