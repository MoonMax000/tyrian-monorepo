{
  "name": "socialweb-favorites-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backends/socialweb-favorites-service",
  "projectType": "application",
  "tags": ["type:backend", "scope:social-network", "language:go"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go run main.go",
        "cwd": "apps/backends/socialweb-favorites-service"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o dist/service .",
        "cwd": "apps/backends/socialweb-favorites-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go test ./...",
        "cwd": "apps/backends/socialweb-favorites-service"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t tyrian/socialweb-favorites-service .",
        "cwd": "apps/backends/socialweb-favorites-service"
      }
    }
  }
}

